<h1>Carrito</h1>

{{#each products}}
<div class="card">
    <img src="{{this.path}}" alt="{{this.title}}">
    <h3>{{this.title}}</h3>
    <p>{{this.description}}</p>
    <a class="btn" href="{{this.id}}/delete">Eliminar</a>
    
</div>

{{/each}}

